[project]
name = "telegram-mcp"
version = "0.1.0"
description = "Read-only Telegram MCP server for AI agents (read channel history, join channels)."
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "fastmcp>=2.13.0.2",
    "telethon>=1.42.0",
]
